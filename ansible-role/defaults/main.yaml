wifi_bw_id: "{{ undef(hint='GUID for item in BitWarden') }}"
hostname: "{{ undef(hint='Hostname of the Raspberry Pi') }}"
runner_user: "{{ undef(hint='User to run the application') }}"
public_key: "{{ undef(hint='Drone public key for deploys') }}"
api_key: "{{ undef(hint='API key for browser to backend communication') }}"
spotify_client_id: "{{ undef(hint='Client ID from Spofity developer portal') }}"
spotify_client_secret: "{{ undef(hint='Client secret from Spotify developer portal') }}"
runner_groups: "{{ runner_user }},gpio,dialout"
runner_home: "/home/{{ runner_user }}"